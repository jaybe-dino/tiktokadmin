-- ═════════════════════════════════════════════════════════════
-- 33 · 유입 자동안내 발송 실패 가시화 — 발송 시도가 provider 미설정/실패로
--   0건일 때도 이력을 남겨 "왜 0건인지" 보이게 한다(기존: 성공 시에만 기록 → 조용한 0).
-- ═════════════════════════════════════════════════════════════
ALTER TABLE channel_sends ADD COLUMN IF NOT EXISTS error text;  -- 실패 사유(미설정/오류). 성공이면 NULL.
