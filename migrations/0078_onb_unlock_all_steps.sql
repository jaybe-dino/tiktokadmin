-- ═════════════════════════════════════════════════════════════
-- 78 · 온보딩 신청서 전 단계 잠금 해제 (BUG-15)
--   · step1~5 를 동시에 작성할 수 있도록 잠긴(locked) 단계를 모두 열어준다.
--   · 이미 제출/승인/반려된 단계는 건드리지 않음.
-- ═════════════════════════════════════════════════════════════

UPDATE onb_steps
   SET status = 'unlocked', unlocked_at = COALESCE(unlocked_at, now())
 WHERE status = 'locked';
